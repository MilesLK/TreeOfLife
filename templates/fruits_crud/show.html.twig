{% extends 'base.html.twig' %}

{% block title %}Fruits{% endblock %}

{% block body %}
    <h1>Fruits</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fruit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fruit.Name }}</td>
            </tr>
            <tr>
                <th>Occupation</th>
                <td>{{ fruit.Occupation }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ fruit.Age }}</td>
            </tr>
            <tr>
                <th>Evenement</th>
                <td>{{ fruit.Evenement }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ fruit.Date ? fruit.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fruits_c_r_u_d_index') }}">back to list</a>

    <a href="{{ path('app_fruits_c_r_u_d_edit', {'id': fruit.id}) }}">edit</a>

    {{ include('fruits_crud/_delete_form.html.twig') }}
{% endblock %}
